# Role to nstall common packages
---
  - name: Install Packages
    apt:
      name: "{{ item }}"
      state: present
      update_cache: yes
    loop:
      - tree
      - vim
      - sl
      - nmap
      - tmux
      - htop
      - curl
      - wget
      - unzip
      - traceroute
      - rsync
    tags:
      - common_packages
