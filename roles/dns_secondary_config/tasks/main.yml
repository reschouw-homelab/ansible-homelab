# Role to configure Bind9 to be Secondary Slave
---
# Copy configuration files
  - name: Copy named.conf
    copy:
      src: named.conf
      dest: /etc/bind/named.conf
      owner: root
      group: bind
      mode: 0644
    tags:
      - dns

# Restart Bind9
  - name: Restart Bind9
    service:
      name: bind9
      state: restarted
    tags:
      - dns

